apiVersion: v1
kind: Service
metadata:
  name: {{ include "mail-and-media-shop.keycloakdb.svcName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mail-and-media-shop.labels" . | nindent 4 }}
    app.kubernetes.io/component: keycloak-db
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: keycloak-db
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: db
      port: 5432
      targetPort: 5432
      protocol: TCP
